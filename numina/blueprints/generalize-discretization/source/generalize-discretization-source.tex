
We should have a discretization for isometries that takes into account the initial worm and area threshold

\begin{definition}
  The list of discretized isometries for the initial worm with the area bound are those discretized isomeries that don't translate the origin to a point so far away from the initial worm that they make the area too large, ie a point not in the LocationRange
\end{definition}